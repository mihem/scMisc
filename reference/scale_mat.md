# scale matrix from pheatmap only internal use

copied from pheatmap for internal use

## Usage

``` r
scale_mat(mat, scale)
```

## Arguments

- mat:

  matrix input

- scale:

  should the values be centered and scaled in row, column direction?
  Allowed: `row`, `column`, `none`

## Value

matrix scaled
